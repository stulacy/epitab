First release:
  - Error handling, allow for no cross-tabulation functions to be provided
  - Update vignette to include an example of a custom model

Further features:
  - Allow a second stratification variable
